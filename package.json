{
  "name": "marked-exp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run -s build && jasmine --config=jasmine.json",
    "test-debug": "npm run -s build && node --inspect-brk node_modules/.bin/jasmine --config=jasmine.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/prismjs": "^1.26.0",
    "marked": "^4.0.12",
    "prismjs": "^1.27.0"
  },
  "devDependencies": {
    "@types/jasmine": "^3.10.3",
    "@types/marked": "^4.0.2",
    "jasmine": "^4.0.2",
    "typescript": "^4.5.5"
  }
}
